# Analytics engine

Future loop:
views → retention → likes → comments → shares → subscribers gained
→ content-format score → next-day topic/template selection.

The system should optimize quality and engagement, not simply upload volume.
