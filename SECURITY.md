# Security

Never commit OAuth secrets, refresh tokens, Meta tokens, passwords or API keys.
Store them in GitHub Actions Secrets. Use 2FA on all owner accounts.
Keep a separate recovery document containing account ownership and recovery
steps. The local computer must not be a required runtime dependency.
